<div class="container">
  <h1>My Tasks</h1>
  <ul>
  <% @tasks.each do |task| %>
    <li check_box><%= (link_to task.title, task_path(task)) %></li>
    <%= link_to "Delete task", task_path(task), method: :delete %>
  <% end %>
</ul>

<%= link_to "➕ Create new task", new_task_path %>

</div>

